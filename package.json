{
  "name": "maybelist",
  "version": "0.0.0",
  "description": "MaybeList loose interpretation of a monadic array type or something like that",
  "keywords": [],
  "license": "ISC",
  "author": "Benjamin Vincent Kasapoglu @Luxcium",
  "main": "index.js",
  "scripts": {
    "prebuild": "npm run pretty && npm run lint && npm run test || exit 1",
    "build": "mkdir -p out/performance/; tsc --generateCpuProfile ./out/performance/ && exit 0",
    "precibuild": "#([ \"$(npm audit)\" = 'found 0 vulnerabilities' ])",
    "cibuild": "npm run build && exit 0",
    "postcibuild": "coveralls < coverage/lcov.info; exit 0",
    "coverage": "jest --coverage && exit 0",
    "prelint": "npx eslint --print-config path/to/main.js | npx eslint-config-prettier-check",
    "lint": "eslint ./src --fix && exit 0",
    "pretty": "prettier -c --write ./src && exit 0",
    "pretest": "npm run pretty; npm run lint",
    "test": "jest && exit 0"
  },
  "devDependencies": {
    "@babel/core": "^7.12.3",
    "@babel/plugin-transform-typescript": "^7.12.1",
    "@types/chai": "^4.2.14",
    "@types/jest": "^26.0.15",
    "@types/node": "^14.14.7",
    "chai": "^4.2.0",
    "chai-jest-mocks": "^1.2.4",
    "coveralls": "^3.1.0",
    "jest": "^26.6.3",
    "nyc": "^15.1.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.4",
    "ts-node": "^9.0.0",
    "tslib": "^2.0.3",
    "typescript": "^4.0.5"
  }
}
